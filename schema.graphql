# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

type Mutation {
  serviceInput(input: ServiceInput!): Service
}

type Query {
  service(id: ID!): Service
  service_all: [Service]
  subscriber(id: ID!): Subscriber
  subscriber_all: [Subscriber]
}

type Service {
  description: String!
  id: ID!
  name: String!
}

type Subscriber {
  id: ID!
  subscription: [Subscription]
  user: User
}

type Subscription {
  expiration: String
  id: ID!
  service: Service!
  start: String!
}

type User {
  email: String!
  id: ID!
}

input ServiceInput {
  description: String!
  name: String!
}
