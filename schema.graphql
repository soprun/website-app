# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

interface UserInterface {
  email: String!
  language: language!
  phone: String
}

type Mutation {
  service(input: ServiceInput!): Service
  subscriber(input: SubscriberInput!): Subscriber
}

type Query {
  service(id: ID!): Service
  serviceAll: [Service]
  signIn(email: String!, password: String!): User
  signUp(email: String!, password: String!): User
  subscriber(id: ID!): Subscriber
  subscriberAll(limit: Int = 10, offset: Int = 0): [Subscriber]
}

type Service {
  description: String!
  id: ID!
  name: String!
}

type Subscriber implements UserInterface {
  email: String!
  id: ID!
  language: language!
  phone: String
  subscription: [Subscription]
}

type Subscription {
  expiration: String
  id: ID!
  service: Service!
  start: String!
}

type User implements UserInterface {
  email: String!
  id: ID!
  language: language!
  phone: String
}

enum language {
  en
  ru
}

input ServiceInput {
  description: String!
  name: String!
}

input SubscriberInput {
  userId: ID!
}
