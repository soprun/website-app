# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

type Mutation {
  service(input: ServiceInput!): Service
  subscriber(input: SubscriberInput!): Subscriber
}

type Query {
  service(id: ID!): Service
  serviceAll: [Service]
  signIn(email: String!, password: String!): User
  signUp(email: String!, password: String!): User
  subscriber(id: ID!): Subscriber
  subscriberAll: [Subscriber]
}

type Service {
  description: String!
  id: ID!
  name: String!
}

type Subscriber {
  id: ID!
  subscription: [Subscription]
  user: User
}

type Subscription {
  expiration: String
  id: ID!
  service: Service!
  start: String!
}

type User {
  email: String!
  id: ID!
  language: language!
  phone: String!
}

enum language {
  en
  ru
}

input ServiceInput {
  description: String!
  name: String!
}

input SubscriberInput {
  userId: ID!
}
